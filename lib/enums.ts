export enum Role {
    STUDENT = 'STUDENT',
    FACULTY = 'FACULTY',
    STAFF = 'STAFF',
    DEPT_OFFICER = 'DEPT_OFFICER',
    ADMIN = 'ADMIN'
}

export enum Status {
    SUBMITTED = 'SUBMITTED',
    IN_PROGRESS = 'IN_PROGRESS',
    ESCALATED = 'ESCALATED',
    RESOLVED = 'RESOLVED'
}
